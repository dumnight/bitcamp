<?xml version="1.1" encoding="UTF-8"?>


<!--XML선언  
	모든 XML문서는 첫줄에 반드시 XML 선언이 있어야 한다
	<?xml ?> < ? xml 처럼 공백이 있으면 안된다
	version속성은 필수항목이고,1.0과 1.1만 가능하다
	
	encoding 속성은 선택항목이고 기본값은 UTF-8이다
	xml문서가 어떤 문자표를 사용하여 인코딩되었는지 설정하는 일을 한다
	
	standalone 속성은 선택 항목이다 기본 값이 true이다
	XML 문서가 다른 문서를 참조하는지 설정한다 
	-->
